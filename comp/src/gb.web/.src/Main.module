' Gambas module file

Public Sub Log(sMsg As String)
  
  Dim hFile As File
  
  hFile = Open "/tmp/session.log" For Append 
  Print #hFile, sMsg
  Close #hFile
  
End

Public Sub Main()

  Dim vVal As Variant
  
  'vVal = JSON.Decode(File.Load("~/test.json"))
  vVal = JSON.Decode("\"bébé\\u0101\"")
  Print JSON.Encode(vVal)
  
End
