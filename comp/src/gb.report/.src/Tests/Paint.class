' Gambas class file

'Export

Static Public Sub Rectangle(X As Float, Y As Float, W As Float, H As Float)
  
  Print "Paint.Rectangle("; CStr(X); ", "; CStr(Y); ", "; CStr(W); ", "; CStr(H); ")"
  Super.Rectangle(X, Y, W, H)
  
End
