' Gambas class file
Private hCon As New connection

Public Sub _new()
  
  Dim hresult As Result
  'Report.debug = True
  hCon.Type = "mysql"
  hCon.User = "root"
  hCon.Name = "test"
  hresult = db.Limit(100).Exec("SELECT * FROM test")
  Print hresult.Count
  ReportHBox3.Data = hresult
End
