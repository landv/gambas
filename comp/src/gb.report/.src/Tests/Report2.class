' Gambas class file
Private hCon As New Connection

Public Sub _new()
  
  Dim hresult As Result
  'Report.debug = True
  hCon.Type = "mysql"
  hCon.User = "root"
  hCon.Name = "test"
  hresult = db.Limit(300).Exec("SELECT * FROM test")
  Print hresult.Count
  ReportHBox3.Data = hresult
End

Public Sub Report2_Data()

  

End
