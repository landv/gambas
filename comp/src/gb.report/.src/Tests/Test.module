' Gambas module file

Public Sub Main()
  
  Dim hReport As New Report
  Dim hLabel As ReportLabel
  
  hLabel = New ReportLabel(hReport)
  hLabel.Padding = ReportPadding["1cm"]
  
  hLabel.Border.Width = "1mm"
  hLabel.Text = "coucou"
  
  hReport.Preview
  
End
