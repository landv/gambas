' Gambas module file

Public Sub Main()
  Dim hpoint As Point = Point(0, 600)
  Dim hpoint2 As Point
  Dim hpmap As MapPoint

hpmap = MTiles.PixelToMapPoint(hpoint, 1)
Print hpmap.Lon, hpmap.Lat
hpoint2 = MTiles.MapPointToPixel(hpmap, 1)
Print hpoint2.x, hpoint2.Y
End

