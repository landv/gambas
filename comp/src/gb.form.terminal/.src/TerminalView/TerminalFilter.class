' Gambas class file

Public Sub Clear()
  
End

Public Sub OutputTo((hView) As TerminalView, (hProcess) As Process, (sData) As String)
  
End

Public Sub InputTo((hView) As TerminalView, (hProcess) As Process, (iKeyCode) As Integer, (sKeyText) As String) As Boolean
  
End

