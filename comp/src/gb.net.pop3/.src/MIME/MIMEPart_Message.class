' Gambas class file

Export

Property Read Body As String
Private $sBody As String

Property Read Headers As Collection
Private $cHeaders As Collection

Property Read Charset As String
Private $sCharset As String

Public Sub _new(sBody As String, cHeaders As Collection)

  $sBody = sBody

  $cHeaders = cHeaders

End

Private Function Body_Read() As String

  Return $sBody

End

Private Function Charset_Read() As String

  If $cHeaders.Exist("charset") Then Return $cHeaders["charset"]

End

Private Function Headers_Read() As Collection

  'If sKey And $cHeaders.Exist(sKey) Then Return $cHeaders[sKey]

  Return $cHeaders

End
