' Gambas module file

Export

Public Sub Optional(Message As String, Button As String, Key As String, Optional Icon As String) As Integer
  
  Return FMessage.Run(Icon, Message, [Button], Key)
  
End
