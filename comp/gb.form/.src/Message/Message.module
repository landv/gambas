' Gambas module file

Export

Public Sub Optional((Message) As String, (Button) As String, (Key) As String, Optional Icon As String, Optional Force As Boolean) As Integer
  
  Return FMessage.Run(Icon, Message, [Button], Key, Force)
  
End
