# Gambas Form File 3.0

{ Form Form
  MoveScaled(48,10,18,18)
  Text = ("Sensor")
  Mask = True
  Stacking = Window.Below
  Sticky = True
  SkipTaskbar = True
  { mnuPopup Menu
    Text = ("Popup")
    Visible = False
    { mnuTaskBar Menu
      Text = ("&Show in task bar")
    }
    { mnuMask Menu
      Text = ("Use mask")
      Checked = True
    }
    { mnuPict Menu
      Text = ("Show picture")
      Checked = True
    }
    { mnuAbout Menu
      Text = ("&About...")
    }
    { Menu1 Menu
    }
    { mnuQuit Menu
      Text = ("&Quit")
      Shortcut = "Esc"
    }
  }
  { Timer1 #Timer
    Enabled = True
    Delay = 5000
  }
}
