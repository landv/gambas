# Gambas Form File 3.0

{ Form Form
  MoveScaled(0,0,64,64)
  Background = &H000000&
  Mouse = Mouse.SizeAll
  Border = False
  Stacking = Window.Above
  SkipTaskbar = True
  Opacity = 50
  { Label1 Label
    MoveScaled(3,3,52,6)
    Font = Font["Bold,+5"]
    Foreground = Color.TextBackground
    Text = ("Cut")
  }
}
