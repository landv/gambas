' Gambas module file

Public Sub Main()

    Dim TS As New TestSuite
    Dim T1 As New Test1
    Dim T2 As New Test2
    Dim Res As New TestResult
    Dim Errs As TestErrors
    Dim Fails As TestErrors

    TS.AddTest(T1)
    TS.AddTest(T2)
    Print TS.CountTestCases()
    TS.Run(Res)
    Errs = Res.Errors
    Fails = Res.Failures
    Debug Res
    Debug TS

End
