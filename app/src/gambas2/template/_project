# Gambas Project File 1.0
Title=$(TITLE)
@
@IF CONSOLE OR GAME
  @IF COMPONENT
Startup=MTest
  @ELSE
Startup=MMain
  @ENDIF
@ENDIF
@IF GRAPHIC
  @IF COMPONENT
Startup=FTest
  @ELSE
Startup=FMain
  @ENDIF
@ENDIF
@
@IF DATABASE
Library=gb.db
@ENDIF
@
@IF NETWORK
Library=gb.net
Library=gb.net.smtp
@ENDIF
@
@IF REGEXP
Library=gb.pcre
@ENDIF
@
@IF SETTINGS
Library=gb.settings
@ENDIF
@
@IF VISUAL
Library=gb.vb
@ENDIF
@
@IF XML
Library=gb.xml
Library=gb.xml.xslt
@ENDIF
@
@IF OPENGL
Library=gb.opengl
@ENDIF
@IF GRAPHIC
  @IF GTK
Library=gb.gtk
  @ELSE
Library=gb.qt
Library=gb.qt.ext
Library=gb.form
    @IF DATABASE
Library=gb.db.form
    @ENDIF
    @IF OPENGL
Library=gb.qt.opengl
    @ENDIF
  @ENDIF
@ENDIF
@
@IF IMAGE
Library=gb.image
@ENDIF
@
@IF GAME
Library=gb.sdl
Library=gb.sdl.sound
@ENDIF
@
@IF TRANSLATE
Translate=1
Language=$(LANGUAGE)
@ENDIF
@IF VISUAL
ControlPublic=1
@ENDIF
@IF COMPONENT
MakeComponent=1
@ENDIF
