dnl ---- configure.ac for gb.adt

AC_INIT(configure.ac)
AC_CONFIG_MACRO_DIR([m4])
GB_INIT(gb.adt)
AC_PROG_LIBTOOL

GB_COMPONENT(
  adt, ADT, gb.adt, [src],
)

AC_OUTPUT( \
Makefile \
src/Makefile \
)

GB_PRINT_MESSAGES
